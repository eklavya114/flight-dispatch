# tzdata ships the IANA timezone database as a Python package.
# Required on Vercel's Ubuntu runtime, which does not install system tzdata.
# The zoneinfo module (Python 3.9+) falls back to this package automatically.
tzdata
